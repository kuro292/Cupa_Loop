<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	 xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_1.xsd"
	 version="6.1">

    <servlet>
        <!-- Aqui se define el nombre del Servlet que hace referencia a los servicios REST -->
        <servlet-name>RESTcupaloop</servlet-name>
        <servlet-class>
            org.glassfish.jersey.servlet.ServletContainer
        </servlet-class>
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <!-- 
                 Aqui va el paquete donde se encuentran las
                 clases Java con los servicios:
            -->
            <param-value>com.greenshift.cupaloop.rest</param-value>
        </init-param>
    </servlet>
    <servlet-mapping>
        <!--Aqui va el nombre del servlet que pusiste al inicio: -->
        <servlet-name>RESTcupaloop</servlet-name>
        <!-- Aqui se define la ruta para llegar a los servicios: -->
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>
</web-app>
